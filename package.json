{
  "name": "war3-model",
  "version": "0.0.1",
  "scripts": {
    "sample": "tsc sample.ts parsers/*.ts && browserify sample.js -d -o sampleOut.js"
  },
  "dependencies": {
    "@types/gl-matrix": "^2.2.34",
    "gl-matrix": "^2.3.2"
  },
  "devDependencies": {
    "@types/node": "^7.0.5"
  }
}
